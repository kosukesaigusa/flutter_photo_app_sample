name: flutter_photo_app_sample
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  cached_network_image: ^3.3.1
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.1
  flutter_hooks: ^0.20.5
  gap: ^3.0.1
  hooks_riverpod: ^2.5.1
  riverpod_generator: ^2.4.0

dev_dependencies:
  custom_lint: ^0.6.4
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  riverpod_lint: ^2.3.10

flutter:
  uses-material-design: true
